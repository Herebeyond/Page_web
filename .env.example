# Environment configuration for PostgreSQL with Symfony ORM Pack

# Database Configuration
# For development environment
DATABASE_URL=postgresql://dev_user:dev_password@postgres:5432/symfony_db_dev?serverVersion=15&charset=utf8

# Application Environment
APP_ENV=dev
APP_DEBUG=true

# Legacy database configuration (if still needed for migration)
DB_HOST=postgres
DB_NAME=symfony_db_dev
DB_USER=dev_user
DB_PASS=dev_password